# BACKEND
PYTHONDONTWRITEBYTECODE=1

# Optional: LLM provider (not required — system works without it)
# LLM_PROVIDER=openai
# OPENAI_API_KEY=your_key_here

# Weather API (Open-Meteo is used by default — NO KEY REQUIRED)
# Uncomment to use a different provider
# WEATHER_API_KEY=
